# 酒馆角色卡编辑器 — 需求文档 v0.2

## 一、项目定位

纯客户端 Web 应用（SPA + PWA），用于编辑 SillyTavern 角色卡及其世界书。后期扩展 AI 对话模块，辅助写卡和测试。

- **部署**：静态站点，零后端
- **存储**：浏览器 IndexedDB（Dexie.js）
- **语言**：中文

## 二、技术选型

| 层 | 选择 |
|---|---|
| 框架 | React 18 + TypeScript |
| 构建 | Vite |
| 样式 | Tailwind CSS + shadcn/ui |
| 路由 | react-router-dom v7 |
| 存储 | Dexie.js（IndexedDB） |
| 表单 | react-hook-form + zod |

---

## 三、数据模型

### 3.1 CharacterCard（角色卡）

```ts
CharacterCard {
  id: string               // uuid
  name: string
  description: string       // 核心角色描述（可含 YAML/HTML）
  personality: string
  scenario: string
  first_mes: string         // 第一条消息
  mes_example: string       // 对话示例
  creatorcomment: string    // 作者备注
  avatar: "none" | string   // 头像引用
  talkativeness: number     // 0-1
  fav: boolean
  tags: string[]

  // spec
  spec: "chara_card_v3"
  spec_version: "3.0"

  // 扩展
  creator: string
  character_version: string
  alternate_greetings: string[]
  group_only_greetings: string[]
  system_prompt: string
  post_history_instructions: string

  // 内嵌世界书（可选，也可绑定独立世界书）
  character_book?: WorldBook

  // Regex 脚本
  regex_scripts: RegexScript[]

  // 深度提示
  depth_prompt: {
    prompt: string
    depth: number     // 0-4
    role: "system" | "user" | "assistant"
  }

  // 卡面图片（base64 dataURL）
  card_image?: string

  created_at: Date
  updated_at: Date
}
```

### 3.2 WorldBook（世界书）

```ts
WorldBook {
  id: string
  name: string
  description?: string
  entries: WorldBookEntry[]
  is_standalone: boolean   // 独立 OR 内嵌于某角色卡

  created_at: Date
  updated_at: Date
}

WorldBookEntry {
  id: number
  keys: string[]
  secondary_keys: string[]
  comment: string           // 条目名称/注释
  content: string           // 条目正文
  constant: boolean
  selective: boolean
  insertion_order: number   // 默认 100
  enabled: boolean
  position: "before_char" | "after_char" | "before_user" | "after_user"
  use_regex: boolean
  extensions: {
    position: number        // 0=before_char, 1=after_char, etc.
    depth: number
    probability: number     // 0-100
    useProbability: boolean
    exclude_recursion: boolean
    prevent_recursion: boolean
    // ... 更多可选
  }
}
```

### 3.3 RegexScript（正则脚本）

```ts
RegexScript {
  id: string
  scriptName: string
  findRegex: string
  replaceString: string
  trimStrings: string[]
  placement: number[]
  disabled: boolean
  markdownOnly: boolean
  promptOnly: boolean
  runOnEdit: boolean
  substituteRegex: number
  minDepth: number | null
  maxDepth: number | null
}
```

### 3.4 Preset（AI 预设，对标酒馆格式）

```ts
Preset {
  id: string
  name: string

  // 采样参数
  temperature: number           // 默认 ~1.0
  frequency_penalty: number     // 默认 0
  presence_penalty: number      // 默认 0
  top_p: number                 // 默认 0.9
  top_k: number                 // 默认 1
  top_a: number                 // 默认 0
  min_p: number                 // 默认 0
  repetition_penalty: number    // 默认 1
  openai_max_context: number
  openai_max_tokens: number

  // 提示词列表
  prompts: PresetPrompt[]

  created_at: Date
  updated_at: Date
}

PresetPrompt {
  identifier: string               // uuid 或特殊标识（main/jailbreak/nsfw 等）
  name: string                     // 显示名称（含 emoji 前缀用于分类）
  enabled: boolean
  injection_position: number       // 0=before, 1=after
  injection_depth: number          // 0-4
  injection_order: number          // 默认 100
  role: "system" | "assistant" | "user"
  content: string                  // 提示词正文
  system_prompt: boolean           // 是否为 system prompt 一部分
  marker: boolean                  // 是否为占位标记（用于注入角色卡内容）
  forbid_overrides: boolean
  injection_trigger?: string[]
}
```

特殊 identifier 约定（对应酒馆注入位置）：
- `main` — 主系统提示词
- `jailbreak` — 越狱/破限
- `nsfw` — NSFW 相关
- `personaDescription` — {{user}}描述标记
- `charDescription` — 角色描述标记
- `charPersonality` — 角色性格标记
- `worldInfoBefore` / `worldInfoAfter` — 世界书注入标记
- `scenario` — 场景标记
- `dialogueExamples` — 对话示例标记

### 3.5 ApiConfig（API 配置）

```ts
ApiConfig {
  id: string
  provider: "openai_compatible" | "google" | "anthropic" | "deepseek"
  name: string                // 自定义名称
  base_url: string
  api_key: string
  default_model: string
  available_models: string[]
}
```

### 3.6 ChatSession & ChatMessage（AI 对话）

```ts
ChatSession {
  id: string
  character_id: string         // 关联角色卡
  preset_id: string            // 使用的预设
  title: string
  messages: ChatMessage[]
  created_at: Date
}

ChatMessage {
  id: string
  role: "user" | "assistant" | "system"
  content: string
  hidden: boolean
  created_at: Date
}
```

### 3.7 实体关系

```
CharacterCard 1──1 WorldBook        (内嵌)
WorldBook     1──n WorldBookEntry
CharacterCard n──m WorldBook        (绑定独立世界书)
CharacterCard 1──n ChatSession
ChatSession   1──n ChatMessage
Preset         独立
ApiConfig      独立
```

---

## 四、页面结构

### 路由设计

```
/                    首页 Dashboard
/editor/:id          角色卡编辑器
/editor/new          新建角色卡
/worldbooks          独立世界书管理
/worldbook/:id       编辑某个独立世界书
/chat                额外菜单 — AI 对话（右上角入口）
/settings            全局设置
```

### 4.1 首页 `/`

**核心功能：**

| 功能 | 说明 |
|---|---|
| 卡片网格 | 展示所有角色卡缩略图（卡面 + 角色名 + 版本号 + 更新时间） |
| 搜索 | 按名称搜索 |
| 标签筛选 | 按 tags 过滤 |
| 新建空白卡 | 创建空角色卡，跳转编辑器 |
| 导入角色卡 | 拖拽/点击上传 .png 或 .json，自动解析 |
| 备份全部数据 | 导出整个 IndexedDB 为 JSON 文件并下载 |
| 恢复数据 | 导入之前的备份文件覆盖/合并 |
| 清空数据 | 二次确认后删除全部（弹窗确认） |
| 删除单卡 | 卡片三点菜单 → 删除（需确认） |

### 4.2 角色卡编辑器 `/editor/:id`

**顶部固定栏：** 返回首页 | 卡名 | [保存] [导出JSON] [导出PNG]

**Tab 分栏：**

**Tab 1 — 基本信息**
- 角色名、版本号、作者
- 标签管理（添加/删除）
- Talkativeness 滑块（0-1）
- 卡面图片（显示当前卡面，支持更换/移除）
- 收藏开关

**Tab 2 — 角色定义**
- `description`（大文本框，monospace）
- `personality`（文本框）
- `scenario`（文本框）
- `first_mes`（文本框）
- `mes_example`（文本框）
- `creatorcomment`（文本框）

**Tab 3 — 开场白**
- `alternate_greetings` 列表
- 每条可编辑，可添加/删除/拖拽排序

**Tab 4 — 世界书**
- 显示当前绑定的世界书名称
- 条目列表：每项显示 comment（名称）、enabled 开关、keys
- 点击展开编辑 content 及详细配置
- 添加/删除条目
- **解绑**：将内嵌世界书提取为独立
- **绑定**：从独立世界书列表中选择一个绑定

**Tab 5 — Regex 脚本**
- 脚本列表，每项显示 scriptName + disabled 状态
- 添加/编辑/删除

**Tab 6 — 深度提示**
- depth_prompt.prompt / depth / role
- system_prompt
- post_history_instructions

### 4.3 世界书独立管理 `/worldbooks`

- 列出所有独立世界书
- 新建空世界书 → 进入编辑器
- 点击进入编辑 `/worldbook/:id`（UI 同角色卡内嵌世界书 Tab，额外多了名称编辑）
- 导入/导出世界书 JSON
- 删除世界书
- 显示哪些角色卡绑定了此书

### 4.4 AI 对话测试 `/chat`（Phase 2，先不做）

**用途：**
1. **辅助写卡** — 使用写卡预设，让 AI 帮助生成/润色角色描述、开场白、世界书条目等
2. **测试角色卡** — 选一张卡 + 选一个预设 → 发消息 → 看 AI 回复是否贴合角色人设

**界面：**

```
┌──────────────────────────────────────────────┐
│ [选择角色卡 ▼]  [选择预设 ▼]  [选择API ▼]    │
├────────────────────┬─────────────────────────┤
│  历史会话列表       │  聊天区域                │
│  ┌──────────────┐  │  ┌───────────────────┐  │
│  │ 会话 1       │  │  │ user: ...          │  │
│  │ 会话 2       │  │  │ assistant: ...     │  │
│  │ [+ 新对话]   │  │  │ user: ...          │  │
│  └──────────────┘  │  │ [输入框] [发送]    │  │
│                    │  └───────────────────┘  │
├────────────────────┴─────────────────────────┤
│ [隐藏选中消息] [删除选中消息] [清空对话]      │
└──────────────────────────────────────────────┘
```

**AI 调用逻辑：**
1. 获取当前 Preset 中所有 `enabled: true` 的 prompts
2. 按 `injection_order` → `injection_position` 排序组装 system prompt
3. 对 `marker: true` 的条目，替换为角色卡对应字段内容
4. 拼接历史未隐藏消息
5. 流式（SSE）请求 API，实时渲染

**消息操作：**
- 隐藏/显示：标记 `hidden`，下次发送时不带入上下文
- 删除：物理删除
- 重新生成：删除上一条 AI 回复，重新请求
- 新对话：创建新 ChatSession

### 4.5 全局设置 `/settings`

**分两块：**

**API 配置：**
- 列表展示已保存的 API 配置
- 添加/编辑/删除
- 服务商选择 + Base URL + API Key（密码框遮罩）
- 模型列表：手动添加 + "从 API 拉取"按钮（调 `/v1/models`）
- 一个设为默认

**预设管理：**
- 预设列表
- 导入酒馆预设 JSON（直接解析以上 Preset 结构）
- 新建/编辑/删除预设
- 编辑器中：
  - 基础采样参数（temperature, top_p 等）
  - prompts 列表：每条可编辑 name / content / enabled / role 等
- 导出预设为酒馆兼容 JSON

---

## 五、导入/导出格式

### 5.1 导入 PNG
1. 读 PNG 二进制 → 查找 IEND chunk 之后的 base64 数据
2. 或查找 tEXt chunk 中 keyword="chara" 的内容
3. Base64 解码 → JSON 解析 → 得角色卡数据
4. 原 PNG 作为 card_image 保留

### 5.2 导入 JSON
1. 解析 JSON → 识别 spec / spec_version
2. 提取所有字段 → 构建 CharacterCard
3. 若有 character_book 则内嵌存储

### 5.3 导出 JSON
序列化 CharacterCard 为 spec v3 格式 JSON → 触发浏览器下载

### 5.4 导出 PNG
card_image (PNG binary) + base64(JSON) → 拼接 → 触发下载为 .png

### 5.5 世界书独立导入/导出
```json
{
  "name": "世界书名称",
  "entries": [ ... ]
}
```

### 5.6 预设导入/导出
兼容酒馆原始格式（见 3.4 Preset 结构），直接导入导出。

---

## 六、UI/UX 要点

- 中文界面
- 深色模式优先（酒馆风格），支持浅色切换
- 桌面端为主，响应式可兼容平板
- 大文本区域等宽字体
- 卡片列表网格布局，显示卡面缩略图
- 导入区域支持拖拽 + 点击
- 离开编辑器时有未保存变更 → 弹窗提醒

---

## 七、开发顺序

| 阶段 | 内容 |
|---|---|
| **Phase 1** | 项目初始化：Vite + React + TS + Tailwind + shadcn/ui + Dexie.js |
| **Phase 2** | 首页 Dashboard：卡片列表、新建、导入、备份/恢复/清空 |
| **Phase 3** | 角色卡编辑器：基本信息 + 角色定义 Tab |
| **Phase 4** | 角色卡编辑器：开场白 + Regex 脚本 + 深度提示 Tab |
| **Phase 5** | 世界书 Tab + 独立世界书管理页 |
| **Phase 6** | JSON/PNG 导入导出 |
| **Phase 7** | 预设管理（设置页中的预设部分） |
| **Phase 8** | AI 对话模块（API 配置 + 聊天界面） |
| **Phase 9** | UI 打磨、错误处理、PWA 配置 |

---

## 八、不做 / 暂缓

- 账号/邀请制（纯客户端无意义）
- 后端/云同步
- 多语言（仅中文）
- AI 图片生成
- 角色卡版本历史 / Diff
- 批量导入/导出
